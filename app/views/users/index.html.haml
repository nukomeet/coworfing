.hero-unit
  %h2= title "Our Worfers"
.row
  .span12
    %ul.thumbnails
      - @users.each do |user|
        = render partial: 'user_avatar', locals: { user: user }

= content_for :javascript do
  %script
    $("img.lazy").lazyload();
